[project]
name = "work-vault-template"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "structlog>=24.1.0",
]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
